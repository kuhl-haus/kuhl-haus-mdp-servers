name: Build Images
permissions:
  contents: read
  pull-requests: write

on:
  push:
    branches:
      - mainline
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest

    permissions:
      packages: write  # IMPORTANT: mandatory for publishing packages
      # https://docs.github.com/en/packages/learn-github-packages/about-permissions-for-github-packages#about-scopes-and-permissions-for-package-registries

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for tags

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Install build dependencies
        run: pip install -r requirements-build.txt

      - name: Determine image tag
        id: determine_tag
        run: |
          # Try to get tag from git
          GIT_TAG=$(git describe --tags --exact-match 2>/dev/null || echo "")
      
          if [ -n "$GIT_TAG" ]; then
            # Remove 'v' prefix if present
            IMAGE_TAG=${GIT_TAG#v}
            echo "Using git tag: $IMAGE_TAG"
          else
            # Install setuptools-scm if not already available
            pip install setuptools-scm >/dev/null 2>&1
      
            # Get version from setuptools-scm with no-local-version
            VERSION=$(python -c "from setuptools_scm import get_version; print(get_version(local_scheme='no-local-version'))" 2>/dev/null)
      
            if [ $? -ne 0 ] || [ -z "$VERSION" ]; then
              echo "Error: Could not determine version from setuptools-scm" >&2
              exit 1
            fi
      
            # Add commit hash as suffix for non-tagged builds
            COMMIT_SHORT=$(git rev-parse --short HEAD)
      
            # Format: version-hash (e.g., 0.1.1.dev2-a1b2c3d)
            IMAGE_TAG="${VERSION}-${COMMIT_SHORT}"
            echo "Using setuptools-scm version: $IMAGE_TAG"
          fi
      
          echo "IMAGE_TAG=$IMAGE_TAG" >> $GITHUB_ENV
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push MDP Server Base Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./base.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdp-server-base:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdp-server-base:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push MDL Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./mdl.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdl-server:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdl-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build and push MDP Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./mdp.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdp-server:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-mdp-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build and push LBA Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./lba.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-lba-server:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-lba-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - name: Build and push WDS Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./wds.Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-wds-server:${{ env.IMAGE_TAG }}
            ghcr.io/${{ github.repository_owner }}/kuhl-haus-wds-server:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
